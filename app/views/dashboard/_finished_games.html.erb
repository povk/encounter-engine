<fieldset>
  <legend>
    Baigti žaidimai
  </legend>
  <% Game.finished.each do |game | %>
    Baigtas žaidimas <b><%= link_to game.name, resource(game) %></b> |
      <%= link_to "Peržiūrėti rezultatus", url(:controller => :game_passings, :action => :show_results, :game_id => game.id) %>
    <br/>
  <% end %>
</fieldset>
